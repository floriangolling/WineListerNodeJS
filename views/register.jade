doctype html
html
    head
        title REGISTER
        style
            include style/css/test.css
    body
        form(action="/", method="GET")
            button(type="submit", class="myButton") Accueil
        form(action="/register",method="POST" )
            p(class="myBox" style="margin-top:15%") REGISTER
            input(type="text",value="",name="prenom", placeholder="user", style="margin-top:2%" required)
            p
            input(type="password", value="",name="password", placeholder="password" required)
            p
            button(type="submit", class="myButton", style="margin-left:45%") Créer son compte