doctype html
html
    head
        title ADD
    style
        include style/css/test.css
    body
        form(action="/", method="GET")
            button(type="submit", class="myButton") Accueil
        p(class="myTitle" style="margin-left:46%") Ajout d'un vin
        form(action="/add",method="POST" )
            input(type="text",value="",name="name" placeholder="Nom du vin" required)
            p
            input(type="text", value="",name="description", placeholder="Description du vin" required)
            p
            input(type="number",value="0",name="quantity", placeholder="Quantité", required)
            p
            button(type="submit", class="myButton", style="margin-left:45%") Ajouter le vin